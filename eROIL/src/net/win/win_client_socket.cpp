#include "net/socket.h"

namespace eroil::net {
    ClientSocket::ClientSocket() {}

    ClientSocket::~ClientSocket() {}

    void ClientSocket::send(const void* buf, const size_t size) {
        
    }

    void ClientSocket::recv(void* buf, const size_t size) {

    }
}